<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.bchainprac">
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />
    <uses-permission android:name="android.permission.SEND_SMS" />
    <uses-permission android:name="android.permission.READ_SMS" /> <!-- the following two permissions are required if you want to take photo in selector -->
    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/icon_round"
        android:label="@string/app_name"
        android:requestLegacyExternalStorage="true"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".view.activity.hospital.AddUserActivity"
            android:windowSoftInputMode="adjustResize"></activity>
        <activity android:name=".view.activity.hospital.HospitalPharmacyActivity" />
        <activity android:name=".view.activity.hospital.UploadImage" />
        <activity android:name=".view.activity.DiseaseActivity" />
        <activity android:name=".view.activity.MedicineActivity" />
        <activity android:name=".view.activity.hospital.HospitalMedicalAnalysisActivity" />
        <activity android:name=".view.activity.hospital.HospitalViewAppointment" />
        <activity android:name=".view.activity.hospital.HospitalMedicalActivity" />
        <activity
            android:name="com.zfdang.multiple_images_selector.ImagesSelectorActivity"
            android:configChanges="orientation|screenSize"
            android:hardwareAccelerated="false" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".view.fragment.MapsActivity"
            android:label="@string/title_activity_maps" />
        <activity android:name=".view.activity.ListHospitalActivity" />
        <activity android:name=".view.activity.MedicalAnalysisActivity" />
        <activity android:name=".view.activity.AccountInfoActivity" />
        <activity android:name=".view.activity.WalletCheck" />
        <activity android:name=".view.activity.ResultActivity" />
        <activity android:name=".view.activity.AppointmentActivity" />
        <activity android:name=".view.activity.MedicalActivity" />
        <activity android:name=".view.activity.HospitalActivity" />
        <activity android:name=".view.activity.hospital.HospitalMainActivity" />
        <activity android:name=".view.activity.admin.AdminMainActivity" />
        <activity android:name=".view.activity.hospital.HospitalSignUpActivity"
            android:windowSoftInputMode="adjustResize"/>
        <activity android:name=".view.activity.hospital.HospitalSignInActivity" />
        <activity android:name=".view.activity.SelectionActivity" />
        <activity android:name=".view.activity.SignUpActivity" />
        <activity android:name=".view.activity.SignInActivity" />
        <activity android:name=".view.base.SliderActivity" />
        <activity android:name=".view.base.BaseActivity" />
        <activity android:name=".view.base.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".view.activity.MainActivity"
            android:screenOrientation="portrait" />
    </application>

</manifest>